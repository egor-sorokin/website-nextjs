export interface GridDataItem {
  id: number;
  url: string;
  image: string;
}

export interface DescriptionItem {
  label: string;
  url?: string;
  text: string;
}

export interface UnorderedListItem {
  id: number;
  url?: string;
  text: string;
  name?: string;
}

